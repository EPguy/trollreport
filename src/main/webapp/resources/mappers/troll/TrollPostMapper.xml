<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trollreport.gg.troll.mapper.TrollPostMapper">

	<select id="getTrollPostCount" parameterType="String" resultType="int">
		SELECT Count(*) FROM troll_board where troller = #{name} 
	</select>
	
	<insert id="insertPost" parameterType="com.trollreport.gg.troll.domain.TrollPostDto">
		INSERT INTO troll_board (
			category
			, title
			, content
			, writer
			, troller
			, viewCnt
			, deleteYn
			, insertTime
			, updateTime
			, deleteTime
		) VALUES (
	        #{category}
	        , #{title}
	        , #{content}
	        , #{writer}
	        , #{troller}
	        , 0
	        , 'N'
	        , NOW()
	        , NULL
	        , NULL
		)
	</insert>
	
	<select id="getPostList" parameterType="String" resultType="com.trollreport.gg.troll.domain.TrollPostDto">
        SELECT * FROM troll_board WHERE REPLACE(troller, ' ', '') = #{name}
    </select>
</mapper>